meta {
  name: List Rules
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/rules
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.data: isDefined
}

script:post-response {
  const data = res.getBody();
  if (data && data.data && data.data.length > 0) {
    bru.setVar("rule_id", data.data[0].id);
  }
}
